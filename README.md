# AeroNet
AeroNet is a compact Python app connecting multiple Hawk-H7 drones via binary Bluetooth. It provides real-time GPS tracking, sensor monitoring, CPU load info, drone-to-drone distance calculation, mapping, flight parameter tuning, and critical safety alerts—optimized for low-bandwidth usage.
